# 📁 Структура проекта

## Обзор

```
SattelishMaps/
├── backend/              # Python backend
├── frontend/             # HTML/CSS/JS frontend
├── database/             # База данных
├── docs/                 # Документация
├── .gitignore           # Игнорируемые файлы
├── .env                 # Переменные окружения (НЕ коммитить!)
└── README.md            # Главный файл проекта
```

## Backend (Python)

```
backend/
├── api/                 # API endpoints (маршруты)
│   ├── __init__.py
│   ├── satellite.py     # Эндпоинты для спутниковых данных
│   ├── statistics.py    # Эндпоинты для статистики
│   └── search.py        # Эндпоинты для поиска
│
├── models/              # Модели базы данных
│   ├── __init__.py
│   ├── satellite_data.py  # Модель спутниковых данных
│   └── user.py           # Модель пользователя (если нужно)
│
├── services/            # Бизнес-логика
│   ├── __init__.py
│   ├── copernicus_service.py  # Работа с Copernicus API
│   ├── image_processing.py    # Обработка изображений
│   └── statistics_service.py  # Расчет статистики
│
├── utils/               # Вспомогательные функции
│   ├── __init__.py
│   ├── validators.py    # Валидация данных
│   └── helpers.py       # Общие функции
│
├── config/              # Конфигурация
│   ├── __init__.py
│   ├── settings.py      # Настройки приложения
│   └── database.py      # Настройки БД
│
├── app.py               # Главный файл приложения
└── requirements.txt     # Python зависимости
```

### Назначение папок Backend:

- **api/** - Все HTTP endpoints (GET, POST, PUT, DELETE)
- **models/** - SQLAlchemy модели для работы с БД
- **services/** - Вся бизнес-логика (работа с API, обработка данных)
- **utils/** - Вспомогательные функции (валидация, форматирование)
- **config/** - Конфигурация (настройки, подключение к БД)

## Frontend (HTML/CSS/JS)

```
frontend/
├── css/                 # Стили
│   ├── main.css         # Основные стили
│   ├── map.css          # Стили для карты
│   └── components.css   # Стили компонентов
│
├── js/                  # JavaScript
│   ├── main.js          # Главный файл
│   ├── map.js           # Логика карты (MapTiler)
│   ├── api.js           # Работа с backend API
│   └── statistics.js    # Логика статистики
│
├── components/          # HTML компоненты
│   ├── header.html      # Шапка сайта
│   ├── sidebar.html     # Боковая панель
│   └── stats-panel.html # Панель статистики
│
├── assets/              # Статические файлы
│   ├── images/          # Изображения
│   ├── icons/           # Иконки
│   └── data/            # JSON данные (если нужно)
│
└── index.html           # Главная страница
```

### Назначение папок Frontend:

- **css/** - Все стили (можно разделить по компонентам)
- **js/** - JavaScript логика (карта, API, статистика)
- **components/** - Переиспользуемые HTML части
- **assets/** - Картинки, иконки, статические данные

## Database

```
database/
├── migrations/          # Миграции БД
│   ├── 001_initial.sql
│   └── 002_add_indexes.sql
│
└── schemas/             # Схемы таблиц
    ├── satellite_data.sql
    └── statistics.sql
```

### Назначение папок Database:

- **migrations/** - SQL скрипты для изменения структуры БД
- **schemas/** - Определение структуры таблиц

## Docs

```
docs/
├── TEAM_RULES.md        # Правила работы команды
├── TECH_STACK.md        # Технологии проекта
├── GETTING_STARTED.md   # Инструкция для новичков
└── PROJECT_STRUCTURE.md # Этот файл
```

## Примеры файлов

### Backend: `api/satellite.py`
```python
from flask import Blueprint, jsonify, request

satellite_bp = Blueprint('satellite', __name__)

@satellite_bp.route('/api/satellite-data', methods=['GET'])
def get_satellite_data():
    # Логика получения данных
    return jsonify({'data': []})
```

### Frontend: `js/map.js`
```javascript
// Инициализация карты MapTiler
function initMap() {
    const map = new maptilersdk.Map({
        container: 'map',
        apiKey: 'YOUR_API_KEY',
        style: maptilersdk.MapStyle.SATELLITE
    });
}
```

## Кто что делает?

### Backend команда
- Работает в папке `backend/`
- Создает API endpoints в `api/`
- Пишет логику в `services/`
- Создает модели в `models/`

### Frontend команда
- Работает в папке `frontend/`
- Создает HTML страницы
- Пишет стили в `css/`
- Пишет JavaScript в `js/`

### Database команда
- Работает в папке `database/`
- Создает схемы таблиц
- Пишет миграции

## Важные правила

1. **НЕ создавайте файлы в корне проекта** (кроме README.md, .gitignore, .env)
2. **Используйте правильные папки** для своих файлов
3. **Называйте файлы понятно**: `satellite_service.py`, не `service.py`
4. **Один файл = одна ответственность**

## Следующие шаги

1. Прочитайте эту структуру
2. Выберите свою часть (backend/frontend/database)
3. Создавайте файлы в правильных папках
4. Коммитьте только свои изменения

**Удачи! 🚀**
