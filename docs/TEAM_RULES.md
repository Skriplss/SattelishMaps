# 📋 Правила работы в команде

## 🚫 Что НЕ делать

### 1. **НЕ пушить код напрямую в main**
- Всегда создавайте отдельную ветку для своей задачи
- Формат названия ветки: `feature/название-задачи` или `fix/название-бага`
- Пример: `feature/add-map-layer`, `fix/api-connection`

### 2. **НЕ коммитить без описания**
- Каждый коммит должен иметь понятное описание
- Плохо: `fix`, `update`, `changes`
- Хорошо: `Добавлена функция загрузки данных из Copernicus API`

### 3. **НЕ коммитить мусор**
- Проверяйте `.gitignore` перед коммитом
- НЕ коммитить: `node_modules/`, `__pycache__/`, `.env`, личные настройки IDE
- Используйте `git status` перед `git add`

### 4. **НЕ удалять чужой код без обсуждения**
- Если нужно изменить код другого разработчика - обсудите в команде
- Используйте комментарии в Pull Request

## ✅ Что НУЖНО делать

### 1. **Работа с Git**

#### Создание новой ветки
```bash
git checkout -b feature/your-task-name
```

#### Перед началом работы - обновите main
```bash
git checkout main
git pull origin main
git checkout feature/your-task-name
git merge main
```

#### Коммит изменений
```bash
git add .
git status  # Проверьте что добавляете!
git commit -m "Понятное описание изменений"
```

#### Отправка на GitHub
```bash
git push origin feature/your-task-name
```

#### Создание Pull Request
1. Зайдите на GitHub
2. Нажмите "Compare & pull request"
3. Опишите что вы сделали
4. Попросите Диму проверить код
5. После одобрения - мержите в main

### 2. **Коммуникация**

- **Перед началом задачи** - напишите в чат что берете её
- **Если застряли** - спросите помощи, не тратьте время зря
- **Закончили задачу** - сообщите команде и создайте PR
- **Нашли баг** - сразу сообщите, не молчите

### 3. **Код**

#### Комментарии
```python
# Плохо
def f(x):
    return x * 2

# Хорошо
def calculate_ndvi(red_band, nir_band):
    """Вычисляет индекс NDVI для спутниковых данных"""
    return (nir_band - red_band) / (nir_band + red_band)
```

#### Именование
- **Переменные**: `user_data`, `satellite_image`, `cloud_coverage`
- **Функции**: `get_satellite_data()`, `calculate_statistics()`
- **Константы**: `API_KEY`, `MAX_CLOUD_COVER`

#### Форматирование
- Python: используйте 4 пробела для отступов
- JavaScript: используйте 2 пробела для отступов
- Всегда добавляйте пустую строку в конце файла

### 4. **Тестирование**

Перед коммитом проверьте:
- [ ] Код запускается без ошибок
- [ ] Нет опечаток в консоли
- [ ] Функционал работает как ожидается
- [ ] Не сломали чужой код

## 🔥 Workflow на хакатоне

### Утро
1. Встреча команды (5 минут)
2. Каждый говорит что будет делать
3. Дима распределяет задачи

### В процессе
1. Взяли задачу → создали ветку
2. Сделали → закоммитили → запушили
3. Создали PR → попросили проверить
4. После одобрения → смержили

### Вечер
1. Встреча команды (10 минут)
2. Показываем что сделали
3. Планируем следующий день

## 🆘 Если что-то пошло не так

### Случайно закоммитили в main
```bash
git reset --soft HEAD~1  # Отменить последний коммит
git checkout -b feature/my-task  # Создать ветку
git commit -m "Описание"
git push origin feature/my-task
```

### Конфликт при merge
1. Не паникуйте
2. Позовите Диму
3. Вместе разберетесь

### Удалили что-то важное
```bash
git reflog  # Посмотреть историю
git checkout <commit-hash>  # Вернуться к нужному состоянию
```

## 📞 Контакты

**Если проблема** - сразу пишите в общий чат или зовите Диму!

---

**Помните**: Мы команда! Лучше спросить и сделать правильно, чем молчать и сломать проект 🚀
