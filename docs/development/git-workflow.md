# 🔀 Разделение файлов между Денисом и Савой

## 📊 Принцип разделения

**Правило:** Каждый работает в своих файлах, чтобы не было конфликтов в Git!

---

## 👨‍💻 ДЕНИС - Файлы карты и API

### ✅ Твои файлы (можешь редактировать):

```
frontend/
├── js/
│   ├── map.js              ← ТВОЙ (логика карты)
│   ├── layers.js           ← ТВОЙ (управление слоями) - создашь позже
│   ├── map-controls.js     ← ТВОЙ (контролы карты) - создашь позже
│   └── api.js              ← ТВОЙ (API запросы) - создашь позже
│
└── css/
    └── map.css             ← ТВОЙ (стили карты)
```

### ⚠️ Общие файлы (координируйтесь с Савой):

```
frontend/
├── index.html              ← ОБЩИЙ (договаривайтесь кто что добавляет)
├── js/
│   └── main.js             ← ОБЩИЙ (точка входа)
└── css/
    └── main.css            ← ОБЩИЙ (базовые стили)
```

---

## 🎨 САВА - Файлы UI/UX и статистики

### ✅ Файлы Савы (он редактирует):

```
frontend/
├── js/
│   ├── statistics.js       ← САВА (графики и статистика)
│   ├── filters.js          ← САВА (фильтры)
│   └── ui.js               ← САВА (UI компоненты)
│
├── css/
│   ├── components.css      ← САВА (стили компонентов)
│   └── responsive.css      ← САВА (адаптивность)
│
└── components/
    ├── header.html         ← САВА (шапка)
    ├── sidebar.html        ← САВА (боковая панель)
    └── stats-panel.html    ← САВА (панель статистики)
```

---

## 🔄 Как работать с общими файлами

### index.html - Разделение зон

```html
<!-- ========== ЗОНА САВЫ: Header ========== -->
<header class="header">
    <!-- Сава редактирует здесь -->
</header>

<!-- ========== ЗОНА САВЫ: Sidebar ========== -->
<aside id="sidebar">
    <!-- Сава редактирует здесь -->
</aside>

<!-- ========== ЗОНА ДЕНИСА: Map ========== -->
<div id="map"></div>
<div class="map-controls">
    <!-- Денис редактирует здесь -->
</div>

<!-- ========== ЗОНА САВЫ: Stats Panel ========== -->
<aside id="stats-panel">
    <!-- Сава редактирует здесь -->
</aside>
```

### main.css - Разделение секций

```css
/* ========== БАЗОВЫЕ СТИЛИ (Сава) ========== */
body, html { ... }

/* ========== HEADER (Сава) ========== */
.header { ... }

/* ========== SIDEBAR (Сава) ========== */
.sidebar { ... }

/* ========== STATS PANEL (Сава) ========== */
.stats-panel { ... }

/* ========== ОБЩИЕ КОМПОНЕНТЫ (Сава) ========== */
.button, .card { ... }
```

### main.js - Разделение импортов

```javascript
// ========== ИМПОРТЫ ДЕНИСА ==========
import SatelliteMap from './map.js';
import { loadSatelliteData } from './api.js';

// ========== ИМПОРТЫ САВЫ ==========
import { initStatistics } from './statistics.js';
import { initFilters } from './filters.js';

// ========== ИНИЦИАЛИЗАЦИЯ (Договариваетесь) ==========
function initApp() {
    // Денис: инициализация карты
    const map = new SatelliteMap('map', API_KEY);
    map.init();
    
    // Сава: инициализация UI
    initStatistics();
    initFilters();
}
```

---

## 📝 Стратегия коммитов

### Вариант 1: Коммитите по очереди (РЕКОМЕНДУЕТСЯ)

```bash
# Денис делает свою работу
git add frontend/js/map.js frontend/css/map.css
git commit -m "Добавлена базовая карта с контролами"
git push

# Потом Сава делает pull и свою работу
git pull
git add frontend/js/statistics.js frontend/css/components.css
git commit -m "Добавлена панель статистики"
git push
```

### Вариант 2: Работаете параллельно в разных файлах

```bash
# Денис коммитит свои файлы
git add frontend/js/map.js frontend/js/api.js frontend/css/map.css
git commit -m "Реализована загрузка спутниковых данных"
git push

# Сава коммитит свои файлы (даже если Денис еще не запушил)
git add frontend/js/statistics.js frontend/css/components.css
git commit -m "Добавлены графики Chart.js"
git push
```

**Если будет конфликт при push:**
```bash
git pull --rebase  # Подтянуть изменения
# Решить конфликты если есть
git push
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

### ❌ НЕ ДЕЛАЙТЕ:

1. **Не редактируйте файлы друг друга** без договоренности
2. **Не делайте `git add .`** (добавит все файлы, включая чужие)
3. **Не коммитите одновременно в один файл**

### ✅ ДЕЛАЙТЕ:

1. **Коммитите только свои файлы:**
   ```bash
   git add frontend/js/map.js
   git commit -m "Описание"
   ```

2. **Перед началом работы делайте pull:**
   ```bash
   git pull
   ```

3. **Договаривайтесь в чате** если нужно изменить общий файл

---

## 🎯 Текущее состояние файлов

### Уже созданные файлы:

| Файл | Владелец | Статус |
|------|----------|--------|
| `index.html` | ОБЩИЙ | ✅ Создан (базовая структура) |
| `css/main.css` | ОБЩИЙ | ✅ Создан (базовые стили) |
| `css/map.css` | ДЕНИС | ✅ Создан |
| `js/main.js` | ОБЩИЙ | ✅ Создан |
| `js/map.js` | ДЕНИС | ✅ Создан |

### Файлы для создания:

**Денис создаст:**
- `js/api.js` - работа с backend
- `js/layers.js` - управление слоями
- `js/map-controls.js` - дополнительные контролы

**Сава создаст:**
- `js/statistics.js` - графики
- `js/filters.js` - фильтры
- `js/ui.js` - UI компоненты
- `css/components.css` - стили компонентов
- `components/header.html` - шапка
- `components/sidebar.html` - боковая панель
- `components/stats-panel.html` - панель статистики

---

## 💡 Рекомендация для хакатона

**Самый простой способ:**

1. **Денис работает с картой** (файлы `map.js`, `api.js`, `layers.js`, `map.css`)
2. **Сава работает с UI** (файлы `statistics.js`, `filters.js`, `components.css`)
3. **Общие файлы** (`index.html`, `main.css`, `main.js`) редактируете **по очереди**
4. **Синхронизируетесь каждые 30-60 минут:**
   ```bash
   git pull
   # Проверяете что ничего не сломалось
   # Продолжаете работу
   ```

---

## 🔥 Быстрая шпаргалка

### Денис коммитит:
```bash
git add frontend/js/map.js frontend/js/api.js frontend/css/map.css
git commit -m "Твое описание"
git push
```

### Сава коммитит:
```bash
git add frontend/js/statistics.js frontend/css/components.css
git commit -m "Его описание"
git push
```

### Оба перед работой:
```bash
git pull
```

---

**Главное правило:** Общайтесь! Если не уверен - спроси Саву в чате! 💬
