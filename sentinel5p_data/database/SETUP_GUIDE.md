# 🗄️ Руководство по настройке базы данных Supabase

Пошаговая инструкция для создания базы данных для хранения атмосферных данных Sentinel-5P.

---

## 📋 Что нужно сделать

1. Создать проект в Supabase
2. Запустить SQL скрипт для создания таблиц
3. Запустить SQL скрипт для создания функций (опционально)
4. Проверить что всё работает

---

## Шаг 1: Создание проекта в Supabase

### 1.1 Регистрация

1. Перейди на [supabase.com](https://supabase.com)
2. Нажми **"Start your project"**
3. Войди через GitHub или создай аккаунт

### 1.2 Создание проекта

1. Нажми **"New Project"**
2. Заполни данные:
   - **Name:** `SatelliteMaps` (или своё название)
   - **Database Password:** Придумай надёжный пароль
     - ⚠️ **ВАЖНО:** Сохрани этот пароль! Он понадобится позже
   - **Region:** Выбери ближайший регион (например, `Europe (Frankfurt)`)
   - **Pricing Plan:** `Free` (для начала)

3. Нажми **"Create new project"**
4. Подожди 1-2 минуты пока проект создаётся

---

## Шаг 2: Запуск SQL схемы

### 2.1 Открыть SQL Editor

1. В левом меню найди **"SQL Editor"** (иконка `</>`)
2. Нажми на неё

### 2.2 Скопировать SQL скрипт

1. Открой файл `database/schema.sql` в твоём проекте
2. Выдели **весь текст** (Ctrl+A)
3. Скопируй (Ctrl+C)

### 2.3 Вставить и запустить

1. В Supabase SQL Editor нажми **"New query"**
2. Вставь скопированный SQL код (Ctrl+V)
3. Нажми **"Run"** (или F5)

### 2.4 Что должно произойти

Ты увидишь сообщения о создании:
- ✅ Extension `postgis` enabled
- ✅ Table `satellites` created
- ✅ Table `measurement_sessions` created
- ✅ Table `no2_measurements` created
- ✅ Table `o3_measurements` created
- ✅ Table `so2_measurements` created
- ✅ Table `aerosol_measurements` created
- ✅ Table `co_measurements` created
- ✅ View `latest_atmospheric_data` created
- ✅ Policies created

**Если увидел ошибки:**
- Прочитай текст ошибки
- Обычно это означает что таблица уже существует
- Можно удалить таблицы и запустить снова (см. раздел "Решение проблем")

---

## Шаг 3: Запуск функций (опционально)

Функции нужны для удобной работы с данными (поиск по радиусу, статистика и т.д.)

1. Открой файл `database/functions.sql`
2. Скопируй весь текст
3. В SQL Editor создай новый запрос
4. Вставь код и нажми **"Run"**

---

## Шаг 4: Проверка

### 4.1 Проверить таблицы

1. В левом меню нажми **"Table Editor"**
2. Ты должен увидеть таблицы:
   - `satellites`
   - `measurement_sessions`
   - `no2_measurements`
   - `o3_measurements`
   - `so2_measurements`
   - `aerosol_measurements`
   - `co_measurements`

### 4.2 Проверить данные

1. Нажми на таблицу `satellites`
2. Ты должен увидеть одну строку: **Sentinel-5P**

### 4.3 Тестовый запрос

Вернись в **SQL Editor** и запусти:

```sql
-- Проверить что всё работает
SELECT * FROM satellites;

-- Проверить партиции
SELECT tablename FROM pg_tables 
WHERE schemaname = 'public' 
AND tablename LIKE '%measurements%'
ORDER BY tablename;
```

Должен увидеть список всех таблиц и партиций.

---

## Шаг 5: Получить API ключи

Эти ключи нужны для подключения из Python скриптов.

1. В левом меню нажми **"Project Settings"** (иконка шестерёнки)
2. Выбери **"API"**
3. Скопируй:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **anon public** ключ (длинная строка)

4. Сохрани их в файл `config.ini`:

```ini
[supabase]
url = https://твой-проект.supabase.co
anon_key = твой_anon_ключ_здесь
```

⚠️ **НЕ ПУБЛИКУЙ** `service_role` ключ! Используй только `anon` ключ в клиентском коде.

---

## 📊 Структура созданной базы данных

### Таблицы

```
satellites                    # Информация о спутниках
  └── measurement_sessions    # Метаданные файлов
        ├── no2_measurements       # Данные NO₂
        ├── o3_measurements        # Данные O₃
        ├── so2_measurements       # Данные SO₂
        ├── aerosol_measurements   # Аэрозольный индекс
        └── co_measurements        # Данные CO
```

### Партиции

Каждая таблица измерений разделена по месяцам:
- `no2_measurements_2024_12` (декабрь 2024)
- `no2_measurements_2025_01` (январь 2025)
- И так далее...

**Зачем?** Для быстрого поиска по датам и экономии места.

### Индексы

Созданы индексы для:
- 📍 Геолокации (GIST индексы для PostGIS)
- 📅 Временных меток
- 🎯 Качества данных (qa_value)
- 🔗 Связей между таблицами

---

## 🔧 Примеры использования

### Из SQL Editor

```sql
-- 1. Посмотреть последние данные NO₂
SELECT 
    timestamp,
    latitude,
    longitude,
    no2_column,
    qa_value
FROM no2_measurements
ORDER BY timestamp DESC
LIMIT 10;

-- 2. Статистика по Киеву (радиус 50км)
SELECT * FROM get_measurements_near_point(
    'NO2',      -- параметр
    50.4501,    -- широта Киева
    30.5234,    -- долгота Киева
    50,         -- радиус в км
    7           -- дней назад
);

-- 3. Средние значения за последнюю неделю
SELECT 
    parameter,
    AVG(value) as avg_value,
    COUNT(*) as measurements
FROM latest_atmospheric_data
GROUP BY parameter;
```

### Из Python

```python
from supabase import create_client

supabase = create_client(
    'https://твой-проект.supabase.co',
    'твой_anon_ключ'
)

# Получить последние данные NO₂
response = supabase.table('no2_measurements')\
    .select('*')\
    .order('timestamp', desc=True)\
    .limit(10)\
    .execute()

print(response.data)
```

---

## 🐛 Решение проблем

### Ошибка: "relation already exists"

Таблица уже создана. Варианты:
1. Пропустить (всё работает)
2. Удалить и создать заново:

```sql
-- ОСТОРОЖНО: Удалит все данные!
DROP TABLE IF EXISTS co_measurements CASCADE;
DROP TABLE IF EXISTS aerosol_measurements CASCADE;
DROP TABLE IF EXISTS so2_measurements CASCADE;
DROP TABLE IF EXISTS o3_measurements CASCADE;
DROP TABLE IF EXISTS no2_measurements CASCADE;
DROP TABLE IF EXISTS measurement_sessions CASCADE;
DROP TABLE IF EXISTS satellites CASCADE;

-- Затем запусти schema.sql снова
```

### Ошибка: "extension postgis does not exist"

PostGIS не включен. Запусти:

```sql
CREATE EXTENSION IF NOT EXISTS postgis;
```

Если не помогло - PostGIS недоступен в твоём плане Supabase (маловероятно).

### Ошибка: "permission denied"

Проверь что ты запускаешь SQL от имени владельца проекта (должно работать автоматически).

### Партиции не создаются

Проверь даты в `schema.sql`. Если сейчас не декабрь 2024, создай партиции для текущего месяца:

```sql
-- Пример для марта 2025
CREATE TABLE no2_measurements_2025_03 PARTITION OF no2_measurements
    FOR VALUES FROM ('2025-03-01') TO ('2025-04-01');
```

---

## 📚 Следующие шаги

После настройки БД:

1. ✅ Настрой `config.ini` с учетными данными
2. ✅ Запусти скрипт загрузки данных
3. ✅ Обработай данные
4. ✅ Загрузи в Supabase
5. ✅ Создай визуализацию

---

## 💡 Полезные ссылки

- [Документация Supabase](https://supabase.com/docs)
- [PostGIS документация](https://postgis.net/docs/)
- [PostgreSQL партиционирование](https://www.postgresql.org/docs/current/ddl-partitioning.html)

---

**Готово!** 🎉 Теперь у тебя есть полноценная база данных для атмосферных данных.
