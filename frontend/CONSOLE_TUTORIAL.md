# 🎮 Как использовать консоль браузера

## Шаг 1: Открой консоль

В браузере (где открыта твоя карта http://localhost:8000) нажми:
- **F12** (самый простой способ)
- Или **Ctrl + Shift + I**
- Или **Правый клик → Inspect (Просмотреть код)**

## Шаг 2: Перейди на вкладку Console

Вверху панели разработчика увидишь вкладки:
- Elements
- **Console** ← Нажми сюда!
- Sources
- Network
- и другие...

## Шаг 3: Найди строку ввода

Внизу консоли будет строка с символом `>` - это место для ввода команд.

Выглядит примерно так:
```
> _  (мигающий курсор)
```

## Шаг 4: Введи команду и нажми Enter

Например, скопируй эту команду:
```javascript
window.map.flyTo([30.5234, 50.4501], 12)
```

**Как скопировать:**
1. Выдели команду мышкой
2. Нажми **Ctrl + C** (скопировать)
3. Кликни в строку ввода консоли (где `>`)
4. Нажми **Ctrl + V** (вставить)
5. Нажми **Enter**

## 🎯 Команды для тестирования

Попробуй по очереди (каждую команду вводи и нажимай Enter):

### 1. Переместить карту в Киев
```javascript
window.map.flyTo([30.5234, 50.4501], 12)
```
**Что должно произойти:** Карта красиво "полетит" в Киев

---

### 2. Переместить карту в Лондон
```javascript
window.map.flyTo([-0.1276, 51.5074], 12)
```
**Что должно произойти:** Карта переместится в Лондон

---

### 3. Увеличить зум
```javascript
window.map.zoomIn()
```
**Что должно произойти:** Карта приблизится

---

### 4. Уменьшить зум
```javascript
window.map.zoomOut()
```
**Что должно произойти:** Карта отдалится

---

### 5. Показать индикатор загрузки
```javascript
window.map.showLoader()
```
**Что должно произойти:** Появится окошко "Загрузка данных..." со спиннером

---

### 6. Скрыть индикатор загрузки
```javascript
window.map.hideLoader()
```
**Что должно произойти:** Окошко загрузки исчезнет

---

### 7. Посмотреть текущие координаты центра карты
```javascript
window.map.currentCenter
```
**Что должно произойти:** В консоли появятся координаты, например: `{lng: 30.5234, lat: 50.4501}`

---

### 8. Посмотреть текущий зум
```javascript
window.map.currentZoom
```
**Что должно произойти:** В консоли появится число, например: `10.5`

---

### 9. Получить границы видимой области
```javascript
window.map.getBounds()
```
**Что должно произойти:** В консоли появятся границы карты (север, юг, запад, восток)

---

## 💡 Полезные советы

### Автодополнение
Начни печатать `window.map.` и нажми **Tab** - консоль покажет доступные методы!

### История команд
Нажми **стрелку вверх ↑** чтобы вернуть предыдущую команду

### Очистить консоль
Нажми **Ctrl + L** или напиши `clear()` и нажми Enter

### Если команда не работает
1. Проверь что карта загрузилась (должно быть сообщение "✅ Карта загружена успешно!")
2. Проверь что скопировал команду полностью
3. Проверь что нет опечаток

---

## 🎓 Что такое эти команды?

Это **JavaScript** - язык программирования для браузера.

- `window.map` - это наш объект карты (мы его создали в main.js)
- `.flyTo()`, `.zoomIn()` - это методы (функции) нашего объекта
- `[30.5234, 50.4501]` - это координаты [долгота, широта]
- `12` - это уровень зума

**Консоль - это как командная строка для веб-страницы!** 🚀

Ты можешь писать любой JavaScript код и он сразу выполнится на странице.

---

## 📸 Что делать дальше?

1. Попробуй все команды выше
2. Поэкспериментируй с разными координатами
3. Попробуй разные уровни зума (от 1 до 20)
4. Скажи мне что получилось, и мы продолжим!

**Следующий этап:** Добавим загрузку реальных спутниковых данных! 🛰️
