# 🚀 Как запустить проект

## Шаг 1: Получить API ключ MapTiler

1. Зайди на https://www.maptiler.com/
2. Нажми "Sign up for free" (бесплатная регистрация)
3. После регистрации зайди в "Account" → "API keys"
4. Скопируй свой API ключ

## Шаг 2: Вставить API ключ

Открой файл `frontend/js/main.js` и замени строку:
```javascript
const MAPTILER_API_KEY = 'get_your_own_OpIi9ZULNHzrESv6T2vL';
```

На:
```javascript
const MAPTILER_API_KEY = 'ТВОЙ_API_КЛЮЧ_СЮДА';
```

## Шаг 3: Запустить локальный сервер

### Вариант 1: Python (если установлен)
```bash
cd frontend
python -m http.server 3000
```

Потом открой в браузере: http://localhost:3000

### Вариант 2: Node.js (если установлен)
```bash
cd frontend
npx http-server -p 3000
```

Потом открой в браузере: http://localhost:3000

### Вариант 3: VS Code Live Server
1. Установи расширение "Live Server" в VS Code
2. Правый клик на `index.html`
3. Выбери "Open with Live Server"

## Шаг 4: Проверить что работает

1. Откройся страница с картой
2. Нажми F12 (открыть консоль разработчика)
3. Ты должен увидеть:
   ```
   🚀 Запуск приложения SattelishMaps...
   🗺️ Инициализация карты...
   ✅ Карта загружена успешно!
   ```

4. Попробуй:
   - Кнопки +/- (зум)
   - Перетаскивание карты мышкой
   - Колесико мыши (зум)
   - Кликнуть по карте (в консоли появятся координаты)

## 🐛 Если что-то не работает

### Карта не загружается
- Проверь API ключ
- Проверь консоль (F12) на ошибки
- Убедись что запущен локальный сервер

### Ошибка CORS
- Нельзя открывать `index.html` напрямую (двойной клик)
- Нужно использовать локальный сервер

### Карта серая
- Проверь интернет соединение
- Проверь API ключ

## 🎮 Команды для консоли

После загрузки страницы открой консоль (F12) и попробуй:

```javascript
// Переместиться в Киев
window.map.flyTo([30.5234, 50.4501], 12)

// Переместиться в Лондон
window.map.flyTo([-0.1276, 51.5074], 12)

// Увеличить зум
window.map.zoomIn()

// Показать индикатор загрузки
window.map.showLoader()

// Скрыть индикатор загрузки
window.map.hideLoader()

// Посмотреть текущие координаты
window.map.currentCenter

// Посмотреть текущий зум
window.map.currentZoom
```
