# 🎨 Переключение стилей карты по теме

## ✅ Что сделано:

### 1. Стили MapTiler DataViz
- **Светлая тема:** `dataviz-light` - минималистичный светлый стиль
- **Темная тема:** `dataviz-dark` - темный стиль для ночного режима

### 2. Автоматическое переключение
- При загрузке страницы карта использует текущую тему
- При переключении темы (кнопка 🌙/☀️) карта автоматически меняет стиль
- Маска Словакии автоматически применяется заново после смены стиля

### 3. Реализация
**В `map.js`:**
- Метод `init(theme)` - принимает тему при инициализации
- Метод `setMapStyle(theme)` - меняет стиль карты динамически
- Автоматическое переприменение маски после загрузки нового стиля

**В `main.js`:**
- Передача текущей темы при инициализации карты
- Вызов `setMapStyle()` при клике на кнопку темы

## 🧪 Проверь:

1. **Обнови страницу** (Ctrl + Shift + R)
2. **Попробуй:**
   - Нажми кнопку переключения темы (🌙/☀️)
   - Карта должна плавно сменить стиль
   - Словакия остается выделенной черной границей
   - Серый оттенок остается на остальном мире

## 🎨 Стили:

### DataViz Light (светлая тема)
- Минималистичный дизайн
- Светлые тона
- Хорошо видны границы и названия

### DataViz Dark (темная тема)
- Темный фон
- Приглушенные цвета
- Комфортно для глаз ночью

---

**Попробуй переключить тему и посмотри как меняется карта!** 🌓
